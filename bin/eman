#!/usr/bin/env ruby

require "thor"
require "eman"

module Eman
  class CLI < Thor

    desc "controller", "name a controller"

    def controller
      generator = Eman::NameGenerator.new('controller')
      generator.run
    end

  end
end

Eman::CLI.start(ARGV)
